<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hubu.work.mybatis.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.hubu.work.mybatis.pojo.User">
    <id column="ID" jdbcType="BIGINT" property="id" />
    <result column="USERNAME" jdbcType="VARCHAR" property="username" />
    <result column="PASSWORD" jdbcType="VARCHAR" property="password" />
    <result column="NICKNAME" jdbcType="VARCHAR" property="nickname" />
    <result column="AVATAR_URL" jdbcType="VARCHAR" property="avatarUrl" />
  </resultMap>
  <select id="selectAll" resultType="com.hubu.work.mybatis.pojo.User">
    select * from user
  </select>
  <select id="findUser" resultType="int">
    select count(*) from user
    <where>
      username=#{username} and password=#{password}
    </where>
  </select>
  <update id="updateImg">
    update user
    <set>
      avatar_url=#{avatarUrl}
    </set>
    <where>
      username=#{username}
    </where>
  </update>
  <select id="findImgPath" resultType="java.lang.String">
    select avatar_url from user where username=#{username}
  </select>
</mapper>